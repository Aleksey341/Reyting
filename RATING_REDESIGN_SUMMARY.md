# Полная переделка визуализации вкладки "Рейтинг"

## 🎯 Краткое резюме

Выполнена полная переделка вкладки "Рейтинг" с целью отображения рейтинга муниципальных образований в виде подробной таблицы с:
- Столбцом названия МО
- Столбцом ФИО главы
- 17 столбцами баллов за критерии (1-17)
- 4 столбцами штрафов (18-21)
- Итоговым столбцом с цветовой дифференциацией по зонам

## 📋 Что было сделано

### ✅ Backend (Python/FastAPI)

1. **Обновлена модель `DimMO`** (`backend/models.py`):
   ```python
   leader_name = Column(String(255))  # ФИО главы МО
   ```

2. **Расширен API эндпоинт** `GET /api/rating` (`backend/routes/rating_routes.py`):
   - Теперь возвращает `leader_name` (ФИО главы)
   - Возвращает `indicators` (словарь баллов по каждому критерию)
   - Возвращает `penalties` (словарь штрафов)

3. **Созданы миграции БД**:
   - `backend/migrations/add_leader_name_to_mo.sql` - добавление колонки
   - `backend/migrations/sample_leader_data.sql` - пример заполнения данных

### ✅ Frontend (React/Tailwind)

**Полностью переделан компонент** `frontend/src/pages/RatingPage.jsx`:

#### Новая структура таблицы:
```
┌──┬──────────────┬─────────────┬────┬────┬───┬────┬────┬────────┬──────┬──────┐
│№ │ Название МО  │ ФИО главы   │ 1  │ 2  │...|17  │18  │19      │20-21 │ИТОГ  │
├──┼──────────────┼─────────────┼────┴────┴───┴────┴────┴────────┴──────┤      │
│  │              │             │   Баллы за критерии                    │      │
│  │              │             ├────┬────┬───┬────┬────┬────────┬──────┤      │
│  │              │             │    │    │...|    │    │  Штрафы        │      │
└──┴──────────────┴─────────────┴────┴────┴───┴────┴────┴────────┴──────┴──────┘
```

#### Цветовая схема:
- **Синий фон** (bg-blue-50) - Баллы за критерии
- **Красный фон** (bg-red-50) - Штрафы
- **Зелёный фон** (bg-green-50) - ИТОГ
- **Белый фон** - Основные столбцы

#### Цветовая дифференциация ИТОГА:
- 🟢 **Зелёный текст** (53-66 баллов) - Зелёная зона (высокая устойчивость)
- 🟡 **Оранжевый текст** (29-52 баллов) - Жёлтая зона (условная устойчивость)
- 🔴 **Красный текст** (0-28 баллов) - Красная зона (низкая устойчивость)

#### Новые функции:
1. **Модальное окно с описанием зон** - кнопка "ℹ️ Описание зон"
2. **Двухуровневые заголовки** таблицы
3. **Горизонтальная прокрутка** для больших таблиц
4. **Адаптивный дизайн** с минимальными шириной столбцов

## 🗂️ Измененные файлы

```
backend/
├── models.py                                      # ✏️ Добавлено: leader_name
└── routes/
    └── rating_routes.py                           # ✏️ Обновлен API эндпоинт

backend/migrations/
├── add_leader_name_to_mo.sql                     # ✨ Новый файл
└── sample_leader_data.sql                        # ✨ Новый файл

frontend/src/pages/
└── RatingPage.jsx                                # 🔄 Полностью переделан

CHANGES_RATING_VISUALIZATION.md                   # ✨ Новая документация
RATING_REDESIGN_SUMMARY.md                        # ✨ Этот файл
```

## 🚀 Как применить изменения

### 1️⃣ Обновление БД

```bash
# Подключитесь к PostgreSQL и примените миграцию
psql -U postgres -d reyting_db -f backend/migrations/add_leader_name_to_mo.sql

# (Опционально) Заполните ФИО глав
psql -U postgres -d reyting_db -f backend/migrations/sample_leader_data.sql
```

Или вручную через SQL:
```sql
ALTER TABLE dim_mo ADD COLUMN leader_name VARCHAR(255);
UPDATE dim_mo SET leader_name = 'ФИО Главы' WHERE mo_name = 'Название МО';
```

### 2️⃣ Перезапуск Backend

```bash
cd backend
python main.py
# Сервер должен быть доступен на http://localhost:8000
```

### 3️⃣ Перезапуск Frontend

```bash
cd frontend
npm install  # если нужны новые пакеты
npm run dev
# Приложение должно быть доступно на http://localhost:5173
```

### 4️⃣ Проверка работоспособности

```bash
# Проверить API ответ
curl "http://localhost:8000/api/rating?period=2024-11&page=1"

# Проверить, что ответ содержит:
# {
#   "data": [{
#     "leader_name": "ФИО главы",
#     "indicators": {"1": 3, "2": 3, ...},
#     "penalties": {"penalty_18": 0, ...},
#     ...
#   }]
# }
```

## 📊 Пример данных в таблице

| № | Название МО | ФИО главы | 1 | 2 | ... | 17 | 18 | 19 | 20 | 21 | ИТОГ |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | Липецк | Ченцов Р.И. | 3 | 3 | ... | 3 | 0 | -1 | 0 | 0 | **61** 🟢 |
| 2 | Елец | Жабин В.П. | 3 | 3 | ... | 2 | 0 | -1 | -1 | 0 | **56** 🟡 |
| 3 | Доб. | Попов А.А. | 3 | 3 | ... | 1 | 0 | -1 | -1 | 0 | **34** 🔴 |

## 📱 Структура зон (из модального окна)

### 🟢 Зелёная зона: 53-66 баллов
**Высокая устойчивость**
Отставка маловероятна, глава МО обладает значительным ресурсом для управления и карьерного роста

### 🟡 Жёлтая зона: 29-52 баллов
**Условная устойчивость**
Существуют риски, требующие коррекции управленческой или политической стратегии

### 🔴 Красная зона: 0-28 баллов
**Низкая устойчивость**
Высокий риск отставки в среднесрочной перспективе, наличие серьёзных системных проблем

## 🔧 Технические детали

### API Ответ

```json
{
  "status": "success",
  "total": 20,
  "page": 1,
  "data": [
    {
      "mo_id": 1,
      "mo_name": "Липецк",
      "leader_name": "Ченцов Р.И.",
      "score_public": 55.0,
      "score_closed": 15.0,
      "score_penalties": -2.0,
      "score_total": 68.0,
      "zone": "green",
      "indicators": {
        "1": 3, "2": 3, "3": 3, "4": 4, "5": 4,
        "6": 4, "7": 4, "8": 3, "9": 4, "10": 5,
        "11": 5, "12": 3, "13": 5, "14": 3, "15": 3,
        "16": 3, "17": 3
      },
      "penalties": {
        "penalty_18": 0,
        "penalty_19": -1,
        "penalty_20": 0,
        "penalty_21": 0
      }
    }
  ]
}
```

### React Компонент

**Путь**: `frontend/src/pages/RatingPage.jsx`

**Ключевые функции**:
- `getScoreTextColor(score)` - определяет цвет текста для ИТОГА
- `getIndicatorScore(item, code)` - получает балл за критерий
- `getPenaltyScore(item, code)` - получает штраф
- Модальное окно для описания зон (управляется через `showZoneModal`)

**State**:
```javascript
const [ratingData, setRatingData] = useState([]);     // Данные таблицы
const [showZoneModal, setShowZoneModal] = useState(false); // Модальное окно
const [currentPage, setCurrentPage] = useState(1);    // Пагинация
const [sort, setSort] = useState('score_total');      // Сортировка
```

## 🎨 CSS классы Tailwind

Используемые классы для визуализации:
- `bg-blue-50` / `bg-red-50` / `bg-green-50` - фоны для разделов
- `border-l-4` - левый бордер для зон в модальном окне
- `sticky top-0` - закрепление заголовка таблицы
- `min-w-[Xpx]` - минимальные ширины столбцов
- `overflow-x-auto` - горизонтальная прокрутка

## ⚠️ Важные примечания

1. **Коды показателей**: В текущей реализации используются простые номера (1-17, penalty_18-21). Если в вашей системе используются другие коды (например, "P001", "P002"), необходимо обновить соответственно.

2. **Заполнение leader_name**: Если в вашей БД отсутствуют ФИО глав, вы можете:
   - Заполнить вручную через SQL UPDATE
   - Загрузить из Excel/CSV файла
   - Оставить поле пустым (выведется "Не указано")

3. **Производительность**: Если в системе большое количество МО (>1000), может потребоваться оптимизация запроса в API (кэширование, индексы).

## 📞 Поддержка и помощь

Если при применении изменений возникнут проблемы:

1. Проверьте логи backend: `python main.py` (должны быть в консоли)
2. Проверьте логи frontend: откройте DevTools (F12) → Console
3. Проверьте сетевые запросы: DevTools → Network
4. Убедитесь, что БД содержит данные в таблице `dim_mo`
5. Убедитесь, что таблицы `fact_indicator` и `fact_penalty` содержат данные

## 🎯 Следующие шаги (рекомендации)

1. **Динамическая загрузка критериев** - загружать список критериев с бэка вместо жесткого кодирования
2. **Экспорт в Excel** - добавить кнопку для экспорта таблицы
3. **Сравнение МО** - возможность выбрать несколько МО и сравнить их показатели
4. **Детальный анализ** - по клику на строку открыть полный анализ показателей
5. **Историческое сравнение** - сравнение показателей между периодами

---

**Статус**: ✅ Готово к использованию
**Дата завершения**: 2024-11-16
**Автор**: Claude Code
