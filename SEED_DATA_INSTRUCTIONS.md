# 🌱 Инструкция по загрузке минимальных данных

После развертывания backend API на Amvera нужно загрузить минимальные справочные данные, чтобы API возвращал непустые результаты.

## ✅ Способ 1: Через psql (самый простой)

```bash
# Подключитесь к БД Amvera
psql -h amvera-alex1976-cnpg-reyting-mo-rw \
     -U reyting_user \
     -d reytingdb \
     -p 5432

# Введите пароль: Qwerty12345

# Скопируйте содержимое etl/seed_minimal_data.sql и выполните его в psql
\i etl/seed_minimal_data.sql

# Или (если psql позволяет):
\c reytingdb
\i /path/to/etl/seed_minimal_data.sql
```

## ✅ Способ 2: Через pgAdmin (если есть доступ)

1. Откройте консоль Amvera → Database → pgAdmin
2. Подключитесь к БД `reytingdb`
3. Query Editor → скопируйте содержимое `etl/seed_minimal_data.sql`
4. Нажмите "Execute"

## ✅ Способ 3: Из Python (локально или в контейнере)

```python
import psycopg2

conn = psycopg2.connect(
    host="amvera-alex1976-cnpg-reyting-mo-rw",
    user="reyting_user",
    password="Qwerty12345",
    database="reytingdb",
    port=5432
)

with open("etl/seed_minimal_data.sql", "r", encoding="utf-8") as f:
    sql = f.read()

cursor = conn.cursor()
cursor.execute(sql)
conn.commit()
cursor.close()
conn.close()

print("✓ Seed data loaded successfully")
```

## 🔍 Проверка после загрузки

```bash
# Проверить API
curl https://reyting-alex1976.amvera.io/

# Должен вернуть JSON с version и docs link

# Проверить rating API
curl https://reyting-alex1976.amvera.io/api/rating | jq

# Должен вернуть массив с МО и их scores
```

## 📊 Что загружается

Seed скрипт (`etl/seed_minimal_data.sql`) создает минимальный набор данных:

| Таблица | Записей | Назначение |
|---------|---------|-----------|
| `dim_methodology` | 1 | Версия методики (v1.0) |
| `dim_indicator` | 1 | Публичный показатель (Численность населения) |
| `dim_penalty` | 1 | Штраф/санкция (для примера) |
| `dim_mo` | 1 | МО (Город N, 600k жителей) |
| `dim_period` | 1 | Текущий месяц |
| `map_scale` | 4 | Шкалы раскраски карты (A/B/C/D зоны) |
| `fact_indicator` | 1 | Значение показателя для МО |
| `fact_summary` | 1 | Итоговый score для МО |

## ⚙️ Если нужны дополнительные данные

Вы можете:

1. **Загрузить полные данные из CSV**:
   ```bash
   python etl/load_assessment_data_v2.py  # Извлечение
   python etl/load_to_db_amvera.py         # Загрузка
   ```

2. **Добавить больше МО**: Скопируйте INSERT в `seed_minimal_data.sql` и замените имена МО

3. **Добавить больше показателей**: Добавьте строки в `dim_indicator` INSERT с разными `code` и `name`

## 🐛 Если что-то не работает

**Ошибка: "psql: command not found"**
- Установите PostgreSQL client: `apt-get install postgresql-client`

**Ошибка подключения к БД**
- Проверьте пароль: `Qwerty12345`
- Проверьте хост: `amvera-alex1976-cnpg-reyting-mo-rw`
- Проверьте порт: `5432`

**API возвращает пусто**
- Убедитесь, что seed скрипт выполнен полностью
- Проверьте логи API: `curl https://domain/logs` или посмотрите в Amvera Console

---

**Версия**: 1.0
**Дата**: 05.11.2025
**Статус**: ГОТОВО ✅
