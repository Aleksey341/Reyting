# 🚀 Быстрый старт: Миграция на Amvera (Полная версия)

## 📊 Что делает эта миграция

1. ✅ Добавляет колонку `leader_name` в таблицу `dim_mo`
2. ✅ Заполняет 20 реальных ФИО глав муниципальных образований Липецкой области
3. ✅ Создает индекс для оптимизации запросов

## 🎯 Выберите способ применения

### **Способ 1️⃣ : Автоматический скрипт (САМЫЙ ПРОСТОЙ) ⭐**

Если у вас установлен `psql` на локальной машине:

```bash
./complete_amvera_migration.sh 'ваш_пароль_из_amvera_console'
```

**Что происходит:**
- ✅ Проверяет подключение к БД
- ✅ Добавляет колонку
- ✅ Заполняет все имена
- ✅ Показывает результаты

---

### **Способ 2️⃣ : Через SSH на Amvera сервер (РЕКОМЕНДУЕТСЯ)**

```bash
# Подключиться к контейнеру
amvera ssh

# Выполнить оба шага в одной сессии psql
psql -U reyting_user -d reytingdb -h amvera-alex1976-cnpq-reyting-mo-rw

# Внутри psql выполнить (скопируйте весь блок):
BEGIN;
ALTER TABLE dim_mo ADD COLUMN IF NOT EXISTS leader_name VARCHAR(255);
CREATE INDEX IF NOT EXISTS idx_dim_mo_leader_name ON dim_mo(leader_name);
UPDATE dim_mo SET leader_name = 'Ченцов Р.И.' WHERE mo_name LIKE '%Липецк%' AND mo_name NOT LIKE '%Липецкий%';
UPDATE dim_mo SET leader_name = 'Жабин В.П.' WHERE mo_name LIKE '%Елец%' AND mo_name NOT LIKE '%Елецкий%';
UPDATE dim_mo SET leader_name = 'Щеглов С.С.' WHERE mo_name LIKE '%Воловский%';
UPDATE dim_mo SET leader_name = 'Рощупкин В.Т.' WHERE mo_name LIKE '%Грязянский%';
UPDATE dim_mo SET leader_name = 'Фалеев В.И.' WHERE mo_name LIKE '%Данковский%';
UPDATE dim_mo SET leader_name = 'Пасынков А.Н.' WHERE mo_name LIKE '%Добринский%';
UPDATE dim_mo SET leader_name = 'Попов А.А.' WHERE mo_name LIKE '%Добровский%';
UPDATE dim_mo SET leader_name = 'Тимохин А.Н.' WHERE mo_name LIKE '%Долгоруковский%';
UPDATE dim_mo SET leader_name = 'Семенихин О.Н.' WHERE mo_name LIKE '%Елецкий%';
UPDATE dim_mo SET leader_name = 'Щедров А.И.' WHERE mo_name LIKE '%Задонский%';
UPDATE dim_mo SET leader_name = 'Иванников В.Ю.' WHERE mo_name LIKE '%Измалковский%';
UPDATE dim_mo SET leader_name = 'Поляков С.О.' WHERE mo_name LIKE '%Краснинский%';
UPDATE dim_mo SET leader_name = 'Телков А.М.' WHERE mo_name LIKE '%Лебедянский%';
UPDATE dim_mo SET leader_name = 'Шабанов К.Ю.' WHERE mo_name LIKE '%Лев-Толстовский%';
UPDATE dim_mo SET leader_name = 'Тодуа Д.В.' WHERE mo_name LIKE '%Липецкий%';
UPDATE dim_mo SET leader_name = 'Семянников Д.Ю.' WHERE mo_name LIKE '%Становлянский%';
UPDATE dim_mo SET leader_name = 'Черников Н.Е.' WHERE mo_name LIKE '%Тербунский%';
UPDATE dim_mo SET leader_name = 'Мазо В.М.' WHERE mo_name LIKE '%Усманский%';
UPDATE dim_mo SET leader_name = 'Плотников А.И.' WHERE mo_name LIKE '%Хлевенский%';
UPDATE dim_mo SET leader_name = 'Сазонов Ю.А.' WHERE mo_name LIKE '%Чаплыгинский%';
COMMIT;

# Проверить результат
SELECT COUNT(*) as leaders_added FROM dim_mo WHERE leader_name IS NOT NULL;
SELECT mo_id, mo_name, leader_name FROM dim_mo WHERE leader_name IS NOT NULL LIMIT 5;

# Выход
\q
```

---

### **Способ 3️⃣ : Через веб-консоль Amvera**

1. Откройте https://console.amvera.ru
2. Авторизуйтесь
3. Перейдите в **Reyting** → **Database** → **PostgreSQL** → **reytingdb**
4. Найдите **SQL Query Editor** или **SQL Console**
5. Скопируйте и вставьте содержимое из **AMVERA_COMPLETE_MIGRATION.md** (раздел Способ 3)
6. Нажмите **Execute**

---

### **Способ 4️⃣ : Несколько отдельных команд (если способ 2 не сработал)**

```bash
# Установить пароль
export PGPASSWORD="ваш_пароль"

# Добавить колонку
psql -U reyting_user -d reytingdb -h amvera-alex1976-cnpq-reyting-mo-rw \
  -c "ALTER TABLE dim_mo ADD COLUMN IF NOT EXISTS leader_name VARCHAR(255);"

# Создать индекс
psql -U reyting_user -d reytingdb -h amvera-alex1976-cnpq-reyting-mo-rw \
  -c "CREATE INDEX IF NOT EXISTS idx_dim_mo_leader_name ON dim_mo(leader_name);"

# Заполнить имена (каждое UPDATE отдельно или все в одной транзакции)
psql -U reyting_user -d reytingdb -h amvera-alex1976-cnpq-reyting-mo-rw \
  -f backend/migrations/populate_leader_names.sql
```

---

## ✅ После миграции: Проверка результата

### Проверка 1️⃣ : В БД (подтвердить, что данные добавлены)
```bash
psql -U reyting_user -d reytingdb -h amvera-alex1976-cnpq-reyting-mo-rw \
  -c "SELECT COUNT(*) FROM dim_mo WHERE leader_name IS NOT NULL;"

# Ожидаемый результат: 20
```

### Проверка 2️⃣ : API (проверить, что API возвращает имена)
```bash
curl "https://reyting-alex1976.amvera.io/api/rating?period=2024-01&page=1&page_size=5"
```

Должен вернуться JSON с leader_name заполненными:
```json
{
  "status": "success",
  "data": [
    {
      "mo_id": 1,
      "mo_name": "Липецк",
      "leader_name": "Ченцов Р.И.",
      "score_total": 61,
      ...
    }
  ]
}
```

### Проверка 3️⃣ : В браузере (визуальная проверка)
1. Откройте https://reyting-alex1976.amvera.io
2. Нажмите на вкладку **"Рейтинг"**
3. Таблица должна загрузиться и показать ФИО глав во втором столбце

---

## 📋 Данные миграции: 20 муниципальных образований

| № | Муниципальное образование | ФИО главы |
|---|---|---|
| 1 | Липецк (город) | Ченцов Р.И. |
| 2 | Елец (город) | Жабин В.П. |
| 3 | Воловский | Щеглов С.С. |
| 4 | Грязянский | Рощупкин В.Т. |
| 5 | Данковский | Фалеев В.И. |
| 6 | Добринский | Пасынков А.Н. |
| 7 | Добровский | Попов А.А. |
| 8 | Долгоруковский | Тимохин А.Н. |
| 9 | Елецкий | Семенихин О.Н. |
| 10 | Задонский | Щедров А.И. |
| 11 | Измалковский | Иванников В.Ю. |
| 12 | Краснинский | Поляков С.О. |
| 13 | Лебедянский | Телков А.М. |
| 14 | Лев-Толстовский | Шабанов К.Ю. |
| 15 | Липецкий | Тодуа Д.В. |
| 16 | Становлянский | Семянников Д.Ю. |
| 17 | Тербунский | Черников Н.Е. |
| 18 | Усманский | Мазо В.М. |
| 19 | Хлевенский | Плотников А.И. |
| 20 | Чаплыгинский | Сазонов Ю.А. |

---

## 🔧 Решение проблем

### ❌ "Column already exists"
**Причина:** Миграция была применена раньше
**Решение:** Это OK! Продолжайте с заполнением имен (UPDATE команды)

### ❌ "UPDATE 0" (обновлено 0 строк)
**Причина:** Названия МО в БД отличаются от ожидаемых
**Решение:** Проверьте точные названия:
```sql
SELECT DISTINCT mo_name FROM dim_mo ORDER BY mo_name;
```

### ❌ "Permission denied"
**Причина:** Пользователь не имеет прав на изменение таблицы
**Решение:** Свяжитесь с поддержкой Amvera

### ❌ "connection refused"
**Причина:** Хост недоступен или нет интернета
**Решение:** Используйте `amvera ssh` для подключения из контейнера

---

## 📚 Дополнительная информация

- **Документ AMVERA_COMPLETE_MIGRATION.md** - полная версия с 4 способами
- **Документ APPLY_MIGRATION_ON_AMVERA.md** - старая версия (только колонка)
- **Файл backend/migrations/populate_leader_names.sql** - SQL миграция для ручного выполнения
- **Файл complete_amvera_migration.sh** - автоматический скрипт

---

## 🎯 Рекомендуемый порядок (для новичков)

1. ✅ **Установить psql** (если его еще нет)
   ```bash
   # Windows: choco install postgresql
   # macOS: brew install postgresql
   # Linux: sudo apt install postgresql-client
   ```

2. ✅ **Получить пароль** из https://console.amvera.ru → Settings → Secrets → DB_PASSWORD

3. ✅ **Запустить скрипт**
   ```bash
   ./complete_amvera_migration.sh 'пароль'
   ```

4. ✅ **Проверить результат**
   ```bash
   curl "https://reyting-alex1976.amvera.io/api/rating?period=2024-01&page=1"
   ```

5. ✅ **Обновить браузер** и посмотреть вкладку "Рейтинг"

---

**Дата:** 16 ноября 2025
**Версия:** 1.0 Complete
**Статус:** ✅ Готово к применению
