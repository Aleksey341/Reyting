# Быстрый старт: Новая визуализация вкладки "Рейтинг"

## 🚀 За 5 минут до работающей системы

### Шаг 1: Обновить БД (2 мин)

```bash
# Подключитесь к PostgreSQL
psql -U postgres -d reyting_db

# Скопируйте и вставьте в консоль psql:
ALTER TABLE dim_mo ADD COLUMN leader_name VARCHAR(255);
CREATE INDEX idx_dim_mo_leader_name ON dim_mo(leader_name);

# Заполните ФИО глав (примеры):
UPDATE dim_mo SET leader_name = 'Ченцов Р.И.' WHERE mo_name = 'Липецк';
UPDATE dim_mo SET leader_name = 'Жабин В.П.' WHERE mo_name = 'Елец';
UPDATE dim_mo SET leader_name = 'Щеглов С.С.' WHERE mo_name = 'Воловский';
```

### Шаг 2: Перезагрузить Backend (1 мин)

```bash
cd /path/to/Reyting/backend
python main.py
```

### Шаг 3: Перезагрузить Frontend (1 мин)

```bash
cd /path/to/Reyting/frontend
npm run dev
```

### Шаг 4: Открыть приложение (1 мин)

- Откройте http://localhost:5173 в браузере
- Перейдите на вкладку "Рейтинг"
- Нажмите кнопку "ℹ️ Описание зон" для информации о зонах

---

## 📋 Что изменилось

### Внешний вид таблицы

**ДО:**
```
Ранг | МО | Публичный | Закрытый | Штрафы | Итого | Зона
```

**ПОСЛЕ:**
```
№ | Название МО | ФИО главы | [Критерии 1-17] | [Штрафы 18-21] | ИТОГ
```

### Цветовая дифференциация ИТОГА

- 🟢 **61** → Зелёная зона (53-66) - Высокая устойчивость
- 🟡 **42** → Жёлтая зона (29-52) - Условная устойчивость
- 🔴 **15** → Красная зона (0-28) - Низкая устойчивость

---

## 🔧 Если что-то не работает

### Ошибка: "Не видны ФИО глав"
→ Заполните поле `leader_name` в БД для нужных МО

### Ошибка: "Нет баллов за критерии"
→ Убедитесь, что в таблице `fact_indicator` есть данные с кодами "1", "2", "3" и т.д.

### Ошибка: "SQLALCHEMY ERROR"
→ Перезагрузите backend: `python main.py`

---

## 📊 Структура таблицы на примере

```
№ | Липецк | Ченцов Р.И. | 3 | 3 | 3 | 4 | 4 | 4 | 4 | 3 | 4 | 5 | 5 | 3 | 5 | 3 | 3 | 3 | 3 | 0 | -1 | 0 | 61
```

**Расшифровка:**
- №: Номер позиции (1)
- Липецк: Название МО
- Ченцов Р.И.: ФИО главы МО
- 3 3 3 4 4 4 4 3 4 5 5 3 5 3 3 3 3: Баллы за критерии 1-17
- 0 -1 0: Штрафы 18-20
- 61: Итоговый балл (🟢 зелёный)

---

## 📂 Какие файлы были изменены

| Файл | Что изменилось |
|------|---|
| `backend/models.py` | ➕ Добавлено поле `leader_name` |
| `backend/routes/rating_routes.py` | 🔄 Расширен API (новые данные) |
| `frontend/src/pages/RatingPage.jsx` | 🔄 Полностью переделана таблица |
| `backend/migrations/` | ✨ Добавлены файлы миграций |

---

## 🎯 Дальше... (опционально)

### Загрузить ФИО из Excel
```python
import pandas as pd

df = pd.read_excel('leaders.xlsx')
for idx, row in df.iterrows():
    # UPDATE dim_mo SET leader_name = row['ФИО'] WHERE mo_name = row['МО']
```

### Экспортировать таблицу в Excel
```javascript
// Добавить кнопку в RatingPage.jsx
<button onClick={exportToExcel}>📥 Скачать Excel</button>
```

### Детальный анализ
```javascript
// При клике на строку показать все показатели МО
<tr onClick={() => showDetailedAnalysis(item.mo_id)}>
```

---

## ✅ Чеклист проверки

- [ ] БД: Добавлена колонка `leader_name`
- [ ] БД: Заполнены ФИО глав (хотя бы для нескольких МО)
- [ ] Backend: Запущен и доступен на http://localhost:8000
- [ ] API: Проверена ответ на http://localhost:8000/api/rating
- [ ] Frontend: Запущен и доступен на http://localhost:5173
- [ ] Таблица: Видны все столбцы (возможна горизонтальная прокрутка)
- [ ] Модальное окно: Открывается при клике на "ℹ️ Описание зон"
- [ ] Цвета: ИТОГ отображается 🟢/🟡/🔴 в зависимости от значения

---

## 📞 Полная документация

Для подробной информации читайте:
- `CHANGES_RATING_VISUALIZATION.md` - Описание всех изменений
- `RATING_REDESIGN_SUMMARY.md` - Полный отчет проекта

---

**Статус**: ✅ Готово
**Время установки**: ~5 минут
**Сложность**: Средняя (требуется доступ к БД)
