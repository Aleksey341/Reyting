# Как выполнить SQL скрипт на Amvera PostgreSQL

## ⚠️ Проблема
Локальный скрипт не может подключиться к Amvera БД (сетевая изоляция).

## ✅ Решение

Выберите один из 3 способов:

---

## Способ 1: Через Amvera Панель управления (Самый простой)

### Шаг 1: Войти в Amvera
1. Перейти на https://amvera.io/
2. Войти в свой аккаунт
3. Выбрать проект `reyting`

### Шаг 2: Найти PostgreSQL БД
1. В левом меню найти "Databases" или "PostgreSQL"
2. Выбрать БД `reyting-db`
3. Найти кнопку "SQL Editor" или "Query Tool"

### Шаг 3: Выполнить SQL
1. Открыть файл `load_official_data.sql` локально
2. Скопировать **весь контент**
3. Вставить в SQL Editor на Amvera
4. Нажать "Execute" или "Run"

**✅ Готово!** SQL выполнен на сервере.

---

## Способ 2: Через SSH на Amvera сервер

### Шаг 1: SSH доступ
```bash
ssh your-username@amvera-alex1976-cnpg-gubernator1-rw
```

### Шаг 2: Использовать psql
```bash
psql -h localhost -U reyting_admin -d reyting-db -f load_official_data.sql
# Введите пароль: Qwerty12345
```

### Шаг 3: Ждём результата
```
✅ SQL выполнен успешно
```

---

## Способ 3: Встроенный скрипт в приложение (Рекомендуемый)

Можно создать специальный endpoint в приложении, который выполнит SQL при вызове:

```python
# backend/routes/admin_routes.py

@router.post("/execute-sql")
async def execute_sql_setup(db: Session = Depends(get_db)):
    """Одноразовая загрузка данных из CSV"""
    try:
        # Вставить содержимое load_official_data.sql
        db.execute(text("""
            -- содержимое SQL скрипта здесь
        """))
        db.commit()
        return {"status": "success", "message": "Data loaded"}
    except Exception as e:
        return {"status": "error", "message": str(e)}
```

Затем вызвать:
```bash
curl -X POST https://reyting-alex1976.amvera.io/api/admin/execute-sql
```

---

## 📋 Что делает SQL скрипт

```sql
-- Для каждого критерия и каждого МО вставляет данные:
INSERT INTO fact_indicator (mo_id, ind_id, period_id, score, ...)
VALUES (1, 15, 1, 0.95, ...)

-- Всего 18 критериев × 20 МО = 360 INSERT операций
```

---

## ✨ После выполнения SQL

### 1. Проверить в Amvera
Выполнить это в том же SQL Editor:
```sql
SELECT COUNT(*) as total_records
FROM fact_indicator
WHERE period_id = 1;
-- Должно быть 360+
```

### 2. Перезагрузить приложение
В панели Amvera нажать "Restart" или "Redeploy"

Когда приложение запустится, сработают миграции:
- `implement_official_methodology()` - создаст 16 критериев
- `calculate_fact_summary_from_indicators()` - рассчитает суммы

### 3. Обновить frontend
```
1. Перейти на вкладку Rating
2. Ctrl+F5 (hard refresh)
3. Вместо нулей появятся оценки ✅
```

---

## 🔐 Данные для подключения

```
Host: amvera-alex1976-cnpg-gubernator1-rw
Port: 5432
Database: reyting-db
User: reyting_admin
Password: Qwerty12345
```

---

## 🆘 Если ничего не работает

### Проверить логи в Amvera:
1. Открыть проект на Amvera.io
2. Найти "Logs" или "Logstream"
3. Искать ошибки типа "fact_indicator", "INSERT", "ERROR"

### Проверить миграции вручную:
```sql
-- Есть ли критерии?
SELECT COUNT(*) FROM dim_indicator WHERE code LIKE 'pub_%';
-- Должно быть 9

-- Есть ли данные?
SELECT COUNT(*) FROM fact_indicator WHERE period_id = 1;
-- Должно быть 360+

-- Есть ли суммы?
SELECT COUNT(*) FROM fact_summary WHERE period_id = 1;
-- Должно быть 20
```

---

## 📁 Файлы для использования

- **load_official_data.sql** - основной SQL скрипт (скопировать содержимое)
- **run_sql_on_amvera.py** - Python скрипт (работает только с SSH доступом)

---

## 🚀 Рекомендуемый порядок

1. ✅ Убедиться, что код на GitHub (commits d6ac3c0, 4c5a2ea)
2. ✅ Перезагрузить приложение на Amvera
3. ✅ **Выполнить load_official_data.sql** (Способ 1 или 2)
4. ✅ Перезагрузить приложение снова
5. ✅ Hard refresh Rating tab
6. ✅ Проверить оценки (не должно быть нулей)

**После этого все должно работать!** ✨
