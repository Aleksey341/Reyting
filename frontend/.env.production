# Production environment variables for Vite build
# These are embedded during 'npm run build'

# API Base URL - use relative path for same-origin requests
# FastAPI backend serves both frontend static files and /api/* endpoints
VITE_API_BASE=/api

# Application config
VITE_APP_TITLE=Reyting Dashboard

# Debug mode (disabled in production)
VITE_DEBUG=false
