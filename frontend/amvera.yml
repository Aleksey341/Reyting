# Deprecated: Use ../amvera-frontend.yml instead
# This file is kept for reference only

# IMPORTANT:
# Use the amvera-frontend.yml in the project root for Amvera deployment
# This old file uses outdated configuration format and is no longer maintained
