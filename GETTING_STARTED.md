# Быстрый старт - Dashboard Липецкой области

Полное руководство по развёртыванию и использованию дашборда оценки эффективности глав МО.

## Минимальные требования

- Docker & Docker Compose 3.8+
- Минимум 4 ГБ оперативной памяти
- 10 ГБ свободного места на диске
- Интернет для скачивания образов Docker

## Вариант 1: Быстрый старт с Docker (5 минут)

### 1. Загрузить проект

```bash
git clone <repo-url> dashboard-project
cd dashboard-project
```

### 2. Запустить приложение

```bash
docker-compose up -d
```

Ждите 2-3 минуты, пока контейнеры стартуют.

### 3. Проверить статус

```bash
docker-compose ps

# Должны быть все сервисы с статусом "Up"
```

### 4. Открыть в браузере

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### 5. Загрузить тестовые данные

```bash
docker-compose exec backend python ../etl/load_csv_data.py
```

## Вариант 2: Локальная разработка (20 минут)

### Backend Setup

```bash
# 1. Перейти в папку backend
cd backend

# 2. Установить зависимости
pip install -r requirements.txt

# 3. Установить переменные окружения
export DATABASE_URL="postgresql://dashboard_user:dashboard_password@localhost:5432/dashboard_db"

# 4. Запустить сервер (требует работающего PostgreSQL)
uvicorn main:app --reload --port 8000
```

Backend будет доступен по адресу `http://localhost:8000`

### Frontend Setup

```bash
# 1. Перейти в папку frontend
cd frontend

# 2. Установить зависимости (может занять несколько минут)
npm install

# 3. Установить переменные окружения
export REACT_APP_API_URL=http://localhost:8000/api

# 4. Запустить dev-сервер
npm start
```

Frontend откроется на `http://localhost:3000`

## Использование приложения

### Главный экран (Карта)

1. Выберите месяц в селекторе "Период" (вверху)
2. Вы увидите таблицу МО с:
   - **Наименование МО**
   - **Зона** (зелёная/жёлтая/красная)
   - **Итоговый балл**
3. Нажмите на МО для просмотра детальной информации

### Рейтинг

1. Перейдите на вкладку "Рейтинг"
2. Отсортируйте по нужному критерию (Итого, Публичный, Название)
3. Используйте стрелки навигации для перемещения по страницам

### Аналитика

1. Откройте вкладку "Аналитика"
2. Отфильтруйте показатели по блокам
3. Изучите описания и метаданные показателей

## Первичная загрузка данных

### Из CSV файлов

```bash
# 1. Подготовить CSV-файлы в формате:
# МО | Период | Код индикатора | Значение | Цель | ...

# 2. Загрузить через API
curl -X POST http://localhost:8000/api/upload/csv_support \
  -F "file=@indicators.csv"

# 3. Проверить статус загрузки
curl http://localhost:8000/api/upload/uploads/1
```

### Через ETL скрипт

```bash
# Загрузить справочные данные (МО, периоды, показатели)
cd etl
python load_csv_data.py
```

## Проверка установки

### 1. Проверить backend

```bash
curl http://localhost:8000/health

# Ожидаемый ответ:
# {"status":"ok"}
```

### 2. Проверить database

```bash
docker-compose exec postgres psql -U dashboard_user -d dashboard_db -c "SELECT COUNT(*) FROM dim_mo;"

# Должно выбрать 20 МО
```

### 3. Проверить frontend

```bash
curl http://localhost:3000

# Должна вернуться HTML страница
```

## Остановка и очистка

```bash
# Остановить контейнеры (данные сохранятся)
docker-compose down

# Полная очистка (удалить базу данных)
docker-compose down -v

# Просмотр логов
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f postgres
```

## Устранение проблем

### Port 3000 или 8000 уже занят

```bash
# Изменить порты в docker-compose.yml
# Или убить процесс:
lsof -ti:3000 | xargs kill -9  # Linux/Mac
netstat -ano | findstr :3000 | findstr LISTENING  # Windows (найти PID)
taskkill /PID <PID> /F  # Windows (убить процесс)
```

### Database connection error

```bash
# 1. Проверить статус контейнера
docker-compose logs postgres

# 2. Убедиться, что postgres стартовал
docker-compose exec postgres pg_isready -U dashboard_user

# 3. Переинициализировать БД
docker-compose down -v
docker-compose up -d postgres
docker-compose up -d  # Остальные сервисы
```

### API возвращает ошибку 500

```bash
# 1. Проверить логи backend
docker-compose logs backend

# 2. Убедиться, что данные загружены
docker-compose exec backend python ../etl/load_csv_data.py

# 3. Перезагрузить backend
docker-compose restart backend
```

### Frontend не загружается

```bash
# 1. Проверить консоль браузера (F12)
# 2. Убедиться, что backend доступен
curl http://localhost:8000/docs

# 3. Проверить переменную окружения API_URL
docker-compose logs frontend | grep API

# 4. Перезагрузить контейнер
docker-compose restart frontend
```

## Следующие шаги

1. **Загрузить реальные данные**
   - Подготовить CSV/Excel файлы от ведомств
   - Загрузить через API `/upload` endpoint

2. **Настроить права доступа** (future)
   - Интегрировать OIDC/LDAP
   - Определить роли пользователей
   - Маскировать чувствительные данные

3. **Настроить расписание загрузок**
   - Использовать cron для ежедневных загрузок
   - Или Airflow для сложных ETL pipelines

4. **Интегрировать с ведомствами**
   - API для прямой подачи данных
   - SFTP для файловых обменов
   - Синхронизация с существующими системами

## Полезные команды

```bash
# Просмотр структуры БД
docker-compose exec postgres psql -U dashboard_user -d dashboard_db -c "\dt"

# Бэкап БД
docker-compose exec postgres pg_dump -U dashboard_user dashboard_db > backup.sql

# Восстановление БД
docker-compose exec -T postgres psql -U dashboard_user dashboard_db < backup.sql

# Интерактивный bash контейнера backend
docker-compose exec backend bash

# Просмотр переменных окружения
docker-compose config
```

## Документация

- [README.md](README.md) - Полное описание проекта
- [backend/README.md](backend/README.md) - Backend документация
- [frontend/README.md](frontend/README.md) - Frontend документация
- [etl/README.md](etl/README.md) - ETL скрипты
- [database_schema.sql](database_schema.sql) - Schema БД

## Поддержка

- Ошибки: GitHub Issues
- Вопросы: GitHub Discussions
- Email: support@example.com

---

**Успешного развёртывания! 🚀**
