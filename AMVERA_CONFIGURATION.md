# Конфигурация Amvera - Подробное описание

## 📋 Структура файлов конфигурации

В проекте есть несколько конфигурационных файлов для Amvera:

```
Reyting/
├── amvera.yml                 # Общая конфигурация (основной контейнер)
├── backend/
│   └── amvera.yml            # Конфигурация Backend (FastAPI)
├── frontend/
│   └── amvera.yml            # Конфигурация Frontend (React)
└── AMVERA_CONFIGURATION.md   # Этот файл
```

---

## 🔧 Конфигурация (основная - amvera.yml)

### Структура

```yaml
version: 1                    # Версия формата конфигурации

meta:
  name: "reyting"            # Название приложения
  description: "..."         # Описание

env: docker                  # Окружение: docker
tool: docker                 # Инструмент: docker

build:                       # Конфигурация сборки
  tool: docker              # Используем docker для сборки
  dockerfile: Dockerfile    # Путь к Dockerfile
  context: .                # Контекст сборки (текущая папка)

run:                         # Конфигурация запуска
  image: "${BUILD_IMAGE}"   # Образ (собранный в build)
  persistenceMount:         # Постоянное хранилище
    - path: /data          # Путь в контейнере
      size: 10Gi           # Размер
  containerPort: 80         # Порт контейнера
  env:                      # Переменные окружения
    - name: DATABASE_URL
      value: "..."

routing:                     # Маршрутизация трафика
  - path: "/"
    targetPort: 80

domains:                     # Внешние домены
  - "reyting.amvera.ru"

secrets:                     # Секреты
  DB_PASSWORD:
    description: "..."
    required: true
```

---

## 🎯 Разделы конфигурации

### 1. META (метаинформация)

```yaml
meta:
  name: "reyting"                    # Внутреннее имя приложения
  description: "Dashboard API"       # Описание для консоли
```

**Описание:**
- `name` - используется в URL, логах, названии контейнера
- `description` - показывается в консоли Amvera

---

### 2. BUILD (сборка образа)

```yaml
build:
  tool: docker                       # Инструмент сборки (всегда docker)
  dockerfile: Dockerfile             # Путь к файлу Dockerfile
  context: .                         # Контекст (от какой папки)
  args:                             # Аргументы сборки (опционально)
    - name: PYTHON_VERSION
      value: "3.11"
```

**Описание:**
- `tool: docker` - использует Docker для сборки (kaniko под капотом)
- `dockerfile` - где находится Dockerfile (относительно context)
- `context` - папка, из которой собирать образ
  - `.` = текущая папка (корень проекта)
  - `backend` = папка backend
- `args` - переменные доступные при сборке (в Dockerfile `ARG PYTHON_VERSION`)

**Примеры путей:**
```yaml
# Если Dockerfile в корне
dockerfile: Dockerfile
context: .

# Если Dockerfile в папке backend
dockerfile: backend/Dockerfile
context: .

# Или альтернативно:
dockerfile: Dockerfile
context: backend
```

---

### 3. RUN (запуск контейнера)

```yaml
run:
  image: "${BUILD_IMAGE}"            # Образ из build (переменная)
  command:                           # Команда запуска (опционально)
    - "uvicorn"
    - "main:app"
  args: []                           # Аргументы команды
  persistenceMount:                  # Постоянные папки
    - path: /data
      size: 10Gi
  containerPort: 8000                # Порт контейнера
  env:                               # Переменные окружения
    - name: DATABASE_URL
      value: "postgresql://..."
```

**Описание:**

#### image
- `"${BUILD_IMAGE}"` - переменная, которая подставляет собранный образ
- Не менять, используется автоматически

#### command
- Команда для запуска приложения
- Переопределяет `CMD` из Dockerfile (если указана)
- Для FastAPI: `uvicorn main:app --host 0.0.0.0 --port 8000`
- Для React: обычно не требуется (используется Nginx из Dockerfile)

#### persistenceMount
- Папки, которые сохраняются между перезагрузками
- `path` - путь в контейнере
- `size` - размер хранилища (10Gi, 1Gi и т.д.)
- Для uploads, logs, databases

#### containerPort
- Порт, на котором слушает приложение внутри контейнера
- FastAPI обычно: 8000
- React обычно: 3000
- Nginx обычно: 80

#### env (переменные окружения)
```yaml
env:
  - name: DATABASE_URL
    value: "postgresql://..."
  - name: DEBUG
    value: "False"
```

---

### 4. ROUTING (маршрутизация)

```yaml
routing:
  - path: "/api"                     # Путь входящего запроса
    targetPort: 8000                 # На какой порт контейнера
    rewrite: ""                      # Переписать ли путь
```

**Описание:**

- `path` - какой путь ловить
  - `/api` - все запросы на /api и подпапки (/api/users и т.д.)
  - `/` - корень (ловит всё)
  - `/docs` - конкретный путь

- `targetPort` - на какой порт контейнера отправить запрос

- `rewrite` - переписать ли путь при отправке
  - `""` или `/` - отправить как есть (без изменений)
  - Пример: запрос `/api/users` → контейнер получит `/api/users`

**Примеры:**

```yaml
# Вариант 1: Backend слушает /api
routing:
  - path: "/api"
    targetPort: 8000
    rewrite: ""

  # Frontend слушает остальное
  - path: "/"
    targetPort: 3000
    rewrite: "/"

# Вариант 2: Один контейнер на всё (всё на одном порту)
routing:
  - path: "/"
    targetPort: 8000
    rewrite: "/"
```

---

### 5. DOMAINS (домены)

```yaml
domains:
  - "reyting.amvera.ru"
  - "www.reyting.amvera.ru"
  - "api.reyting.amvera.ru"
```

**Описание:**

- Внешние домены, на которых доступно приложение
- Автоматически выдаются SSL сертификаты
- Все домены должны указывать на одно приложение (Amvera) или быть настроены в DNS

**Примеры:**
```yaml
domains:
  - "my-app.amvera.ru"              # Поддомен Amvera (бесплатно)
  - "my-app.com"                    # Свой домен (требует DNS настройки)
```

---

### 6. SECRETS (секреты)

```yaml
secrets:
  DB_PASSWORD:
    description: "PostgreSQL password"
    required: true
```

**Описание:**

- Для хранения чувствительных данных
- Значения устанавливаются в консоли Amvera (Settings > Environment Variables)
- В конфигурации указывается только ссылка на них: `${DB_PASSWORD}`

**Использование в env:**
```yaml
env:
  - name: DATABASE_URL
    value: "postgresql://user:${DB_PASSWORD}@host:5432/db"
```

**Установка в консоли Amvera:**
1. Откройте приложение в консоли
2. Settings → Environment Variables
3. Добавьте переменную: `DB_PASSWORD = your_actual_password`

---

### 7. LOGGING (логирование)

```yaml
logging:
  level: "INFO"                      # DEBUG, INFO, WARNING, ERROR
  format: "text"                     # text или json
```

**Описание:**

- `level` - уровень логирования
  - `DEBUG` - всё, включая debug информацию
  - `INFO` - основная информация
  - `WARNING` - предупреждения
  - `ERROR` - только ошибки

- `format`
  - `text` - читаемый текстовый формат
  - `json` - JSON для обработки логов

---

### 8. LIMITS (лимиты)

```yaml
limits:
  startupTimeout: 5                  # Минут на запуск
  gracefulShutdownTimeout: 30        # Секунд на корректное завершение
```

**Описание:**

- `startupTimeout` - максимальное время на запуск контейнера
  - Если контейнер не стартанул за это время - кидается ошибка
  - Для FastAPI: 2-5 минут
  - Для React: 1-2 минуты

- `gracefulShutdownTimeout` - время для корректного завершения
  - Контейнеру дается это время завершить текущие запросы
  - Потом силой заканчивается
  - Обычно: 10-30 секунд

---

### 9. RESOURCES (ресурсы)

```yaml
resources:
  memory: "512Mi"                    # Оперативная память
  cpu: "0.5"                         # CPU cores
```

**Описание:**

- `memory` - сколько RAM выделить контейнеру
  - `256Mi` - 256 мегабайт (для React)
  - `512Mi` - 512 мегабайт (для FastAPI)
  - `1Gi` - 1 гигабайт (для тяжёлых приложений)

- `cpu` - сколько CPU ядер
  - `0.25` - четверть ядра
  - `0.5` - половина ядра
  - `1` - одно ядро

**Рекомендации:**
```
Frontend (React):
  memory: 256Mi
  cpu: 0.25

Backend (FastAPI):
  memory: 512Mi
  cpu: 0.5

Database:
  memory: 1Gi
  cpu: 1
```

---

### 10. HEALTHCHECK (проверка здоровья)

```yaml
healthcheck:
  path: /health                      # Endpoint для проверки
  interval: 30                       # Интервал (секунды)
  timeout: 10                        # Таймаут (секунды)
  successThreshold: 1                # Успешных подряд
  failureThreshold: 3                # Неудачных подряд
```

**Описание:**

- `path` - какой endpoint проверять
  - `/health` - для API
  - `/` - для веб-сайта
  - `/api/health` - для REST API

- `interval` - как часто проверять (в секундах)
  - Обычно: 30 секунд

- `timeout` - как долго ждать ответа (в секундах)
  - Обычно: 5-10 секунд

- `successThreshold` - сколько успешных проверок подряд = контейнер здоров
  - Обычно: 1

- `failureThreshold` - сколько неудачных проверок подряд = контейнер нездоров
  - Обычно: 3
  - Контейнер перезагружается после этого количества неудач

**Примеры:**
```yaml
# Для API
healthcheck:
  path: /health
  interval: 30
  timeout: 10
  failureThreshold: 3

# Для веб-сайта
healthcheck:
  path: /
  interval: 30
  timeout: 10
```

---

## 📊 Пример полной конфигурации для Backend

```yaml
version: 1

meta:
  name: "reyting-backend"
  description: "FastAPI backend for dashboard"

env: docker
tool: docker

build:
  tool: docker
  dockerfile: backend/Dockerfile
  context: .

run:
  image: "${BUILD_IMAGE}"

  command:
    - "uvicorn"
    - "main:app"
    - "--host"
    - "0.0.0.0"
    - "--port"
    - "8000"

  persistenceMount:
    - path: /app/uploads
      size: 10Gi

  containerPort: 8000

  env:
    - name: DATABASE_URL
      value: "postgresql://reyting_user:${DB_PASSWORD}@amvera-alex1976-cnpq-reyting-mo-rw:5432/reytingdb"
    - name: DEBUG
      value: "False"
    - name: PYTHONUNBUFFERED
      value: "1"

  resources:
    memory: "512Mi"
    cpu: "0.5"

  healthcheck:
    path: /health
    interval: 30
    timeout: 10
    successThreshold: 1
    failureThreshold: 3

routing:
  - path: "/api"
    targetPort: 8000
    rewrite: ""
  - path: "/docs"
    targetPort: 8000
    rewrite: ""

domains:
  - "api.reyting.amvera.ru"

secrets:
  DB_PASSWORD:
    description: "PostgreSQL password for reyting_user"
    required: true

logging:
  level: "INFO"
  format: "json"

limits:
  startupTimeout: 5
  gracefulShutdownTimeout: 30
```

---

## 🚀 Как использовать конфигурацию

### Вариант 1: Через консоль Amvera (UI)

1. Откройте консоль https://console.amvera.ru
2. Создайте новое приложение
3. Выберите "Docker" как окружение
4. Заполните параметры формы (они будут преобразованы в amvera.yml)
5. Deploy

### Вариант 2: Загрузить готовый amvera.yml

1. Скачайте amvera.yml с GitHub
2. Отредактируйте (домены, пароли и т.д.)
3. Загрузите в консоль Amvera (если поддерживается)

### Вариант 3: GitHub Integration

1. Залейте amvera.yml в репозиторий
2. Подключите GitHub к Amvera
3. Amvera автоматически прочитает конфигурацию и развернёт

---

## ✅ Проверка конфигурации

```bash
# Валидировать YAML синтаксис
yamllint amvera.yml

# Или просто открыть в редакторе и проверить:
# - Правильные отступы (2 пробела)
# - Все необходимые поля присутствуют
# - Нет опечаток в ключах
```

---

## 📞 Поддержка

- Документация Amvera: https://amvera.ru/docs
- YAML синтаксис: https://yaml.org/
- Docker Dockerfile: https://docs.docker.com/engine/reference/builder/

---

**Последнее обновление**: 2024-11-04
**Версия**: 1.0.0
