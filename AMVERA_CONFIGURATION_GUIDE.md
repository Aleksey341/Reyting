# 🎯 Руководство по выбору конфигурации Amvera

## ❓ Что выбирать в консоли Amvera

### Шаг 1: Выберите окружение

```
Окружение: docker  ✓ ВЫБИРАЙТЕ DOCKER
```

**Почему Docker?**
- Ваше приложение упаковано в Docker образы (Dockerfile есть)
- Самый гибкий способ развертывания
- Поддерживает FastAPI + React + PostgreSQL

---

### Шаг 2: Выберите инструмент

```
Инструмент: docker  ✓ ВЫБИРАЙТЕ DOCKER
```

**Что это значит?**
- Docker используется для сборки образа из Dockerfile
- На Amvera под капотом работает Kaniko (быстрая сборка Docker образов)

---

### Шаг 3: BUILD (Сборка)

#### 3.1 Dockerfile

```
dockerfile: Dockerfile  ← ПУТЬ К ВАШЕМУ DOCKERFILE
```

**Важно! Путь зависит от того, что вы разворачиваете:**

**Для Backend (FastAPI):**
```
dockerfile: backend/Dockerfile
```

**Для Frontend (React):**
```
dockerfile: frontend/Dockerfile
```

**Для единого контейнера (Nginx + всё вместе):**
```
dockerfile: Dockerfile
```

#### 3.2 Build Context

```
context: .   ← ВСЕГДА ТОЧКА (текущая папка = корень репо)
```

**Это означает:**
- Amvera будет собирать образ из корневой папки вашего репо
- Dockerfile может быть в подпапке (backend/, frontend/)
- Контекст остаётся `.` в любом случае

**Почему не `backend/` или `frontend/`?**
- Потому что ваш Dockerfile ссылается на файлы в других папках
- Например, `backend/Dockerfile` ссылается на `backend/main.py`
- Если контекст будет `backend/`, то путь `backend/main.py` не найдётся

---

### Шаг 4: RUN (Запуск)

#### 4.1 Image

```
image: ${BUILD_IMAGE}  ← ВСЕГДА ЭТА ПЕРЕМЕННАЯ
```

**Что это значит?**
- Используется образ, который был собран в BUILD шаге
- `${BUILD_IMAGE}` - это автоматическая переменная Amvera
- Не меняйте, это работает автоматически

#### 4.2 Command (Команда)

```
Выберите: skip  ← ДА, ПРОПУСТИТЕ КОМАНДУ
```

**Почему пропускать?**
- В вашем Dockerfile уже есть `CMD`
- Дополнительная команда переопределит CMD из Dockerfile
- Если CMD уже в Dockerfile - команду не нужно указывать

**Но если хотите переопределить:**

**Для Backend:**
```
command: uvicorn
args: main:app --host 0.0.0.0 --port 8000
```

**Для Frontend:**
```
command: serve
args: -s build -l 3000
```

#### 4.3 Args (Аргументы)

```
Выберите: skip  ← ПРОПУСТИТЕ, ЕСЛИ COMMAND = skip
```

**Когда нужны args?**
- Только если вы указали command выше
- Аргументы команды (флаги, параметры)

#### 4.4 persistenceMount (Постоянное хранилище)

```
/data  ← ПУТЬ В КОНТЕЙНЕРЕ (опционально)
```

**Что это?**
- Папка, которая сохраняется между перезагрузками контейнера
- Используется для: файлов, логов, БД, uploads

**Выбор пути зависит от приложения:**

**Для Backend (FastAPI):**
```
/app/uploads  или  /app/data  (10Gi)
```

**Для Frontend (React):**
```
НЕ нужно (статические файлы не требуют сохранения)
```

**Для PostgreSQL:**
```
/var/lib/postgresql/data  (20Gi+)
```

**НО! Важно:**
- PostgreSQL уже запущена на Amvera (уже создана)
- Для backend/frontend обычно не требуется persistenceMount
- Если не нужно - оставьте пусто или удалите

#### 4.5 containerPort (Порт контейнера)

```
containerPort: 80  ← ЗАВИСИТ ОТ ПРИЛОЖЕНИЯ
```

**Какой порт выбирать?**

**Для Backend (FastAPI):**
```
containerPort: 8000
```
Потому что в `backend/main.py` запускается на 8000.

**Для Frontend (React в Nginx):**
```
containerPort: 3000  или  80
```
Зависит от вашего Dockerfile. Обычно Nginx слушает 80.

**Для БД:**
```
containerPort: 5432
```
Но БД уже на Amvera, вам не нужно это настраивать!

---

## 📊 БЫСТРАЯ ТАБЛИЦА ВЫБОРА

### Вариант A: Развертываете Backend (FastAPI)

```
Окружение: docker              ✓
Инструмент: docker             ✓

BUILD:
  dockerfile: backend/Dockerfile
  context: .

RUN:
  image: ${BUILD_IMAGE}         ✓
  command: skip
  args: skip
  persistenceMount: (пусто или удалите)
  containerPort: 8000
```

### Вариант B: Развертываете Frontend (React)

```
Окружение: docker              ✓
Инструмент: docker             ✓

BUILD:
  dockerfile: frontend/Dockerfile
  context: .

RUN:
  image: ${BUILD_IMAGE}         ✓
  command: skip
  args: skip
  persistenceMount: (пусто или удалите)
  containerPort: 80  (или 3000)
```

### Вариант C: Развертываете оба (один контейнер)

Нужно создать единый Dockerfile или использовать два отдельных контейнера.

---

## 🔍 ПОШАГОВЫЙ ПРИМЕР: Frontend

Допустим, вы развертываете Frontend.

### Шаг 1: Выбираем окружение

```
[✓] docker
```

### Шаг 2: Выбираем инструмент

```
[✓] docker
```

### Шаг 3: BUILD

**3a. dockerfile**

Ищем Dockerfile для frontend в проекте:
```
Reyting/
  ├── frontend/
  │   ├── Dockerfile        ← ОТ СЮДА
  │   ├── package.json
  │   └── src/
```

Вводим путь:
```
dockerfile: frontend/Dockerfile
```

**3b. context**

Остаётся по умолчанию:
```
context: .
```

### Шаг 4: RUN

**4a. image**

Оставляем как есть:
```
image: ${BUILD_IMAGE}
```

**4b. command**

Ищем в `frontend/Dockerfile` строку `CMD` или `ENTRYPOINT`:

Если в Dockerfile есть:
```dockerfile
CMD ["serve", "-s", "build", "-l", "3000"]
```

То в консоли выбираем:
```
command: skip
```

Если хотим переопределить:
```
command: serve
args: -s build -l 3000
```

**4c. args**

Если `command: skip`, то `args: skip`.

**4d. persistenceMount**

Frontend не нуждается в постоянном хранилище (нет файлов для сохранения), поэтому:
```
(оставляем пусто или удаляем)
```

**4e. containerPort**

Смотрим в `frontend/Dockerfile` на EXPOSE:

```dockerfile
EXPOSE 3000
```

Вводим:
```
containerPort: 3000
```

Или если Nginx:
```dockerfile
EXPOSE 80
```

Вводим:
```
containerPort: 80
```

---

## 🎯 ПОШАГОВЫЙ ПРИМЕР: Backend

Допустим, вы развертываете Backend.

### Шаги 1-2: Окружение и инструмент

```
[✓] docker
[✓] docker
```

### Шаг 3: BUILD

**3a. dockerfile**

```
dockerfile: backend/Dockerfile
```

**3b. context**

```
context: .
```

### Шаг 4: RUN

**4a. image**

```
image: ${BUILD_IMAGE}
```

**4b. command**

Смотрим в `backend/Dockerfile`:

```dockerfile
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

Если оставляем как в Dockerfile:
```
command: skip
```

**4c. args**

```
args: skip
```

**4d. persistenceMount**

Если backend сохраняет файлы (uploads):
```
/app/uploads  (размер: 10Gi)
```

Если нет:
```
(пусто)
```

**4e. containerPort**

```
containerPort: 8000
```

---

## ⚙️ ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ

### Где добавлять переменные?

В этой же форме конфигурации должна быть секция:

```
Environment Variables (или env):
  - DATABASE_URL = postgresql://reyting_user:PASSWORD@host:5432/reytingdb
  - DEBUG = False
  - PYTHONUNBUFFERED = 1
```

### Для Backend:

```
DATABASE_URL: postgresql://reyting_user:YOUR_PASSWORD@amvera-alex1976-cnpq-reyting-mo-rw:5432/reytingdb
DEBUG: False
PYTHONUNBUFFERED: 1
```

### Для Frontend:

```
REACT_APP_API_URL: https://api.reyting.amvera.ru/api
NODE_ENV: production
```

---

## 🔐 СЕКРЕТЫ

### Где устанавливать пароли?

**НЕ В DOCKERFILE И НЕ В ПЕРЕМЕННЫХ ВИДИМОЙ ЧАСТИ!**

**В консоли Amvera:**
1. Откройте приложение
2. Settings → Environment Variables → Special (или Secrets)
3. Добавьте: `DB_PASSWORD = ваш_пароль`

**Потом в переменных используйте:**
```
DATABASE_URL: postgresql://reyting_user:${DB_PASSWORD}@host/db
```

---

## ✅ ИТОГОВЫЙ ЧЕКЛИСТ

Перед тем как нажать Deploy:

- [ ] Выбрал окружение: **docker**
- [ ] Выбрал инструмент: **docker**
- [ ] Указал правильный путь к Dockerfile (`backend/`, `frontend/` или корень)
- [ ] context: **всегда точка `.`**
- [ ] image: **${BUILD_IMAGE}**
- [ ] command: **skip** (если CMD в Dockerfile)
- [ ] containerPort: **правильный порт** (8000, 3000, 80)
- [ ] persistenceMount: **пусто** (если не нужно) или **/app/uploads**
- [ ] Environment Variables: **добавлены**
- [ ] DB_PASSWORD: **установлен в Secrets**
- [ ] Dockerfile существует в репозитории
- [ ] Все зависимости в requirements.txt (Python) или package.json (Node)

---

## 🆘 ЧАСТЫЕ ОШИБКИ

### Ошибка 1: "Dockerfile not found"

**Причина:** Неправильно указан путь.

**Решение:**
```
✗ dockerfile: Dockerfile        (ищет в корне)
✗ dockerfile: /backend/Dockerfile (слеш в начале)

✓ dockerfile: backend/Dockerfile  (правильно)
```

### Ошибка 2: "Cannot find module / import"

**Причина:** Контекст неправильный.

**Решение:**
```
✗ context: backend/
✓ context: .
```

Если context = `backend/`, то Dockerfile не видит файлы из других папок.

### Ошибка 3: "Connection refused on port 8000"

**Причина:** Неправильный containerPort или приложение не слушает этот порт.

**Решение:**
- Проверить, на каком порту слушает приложение в Dockerfile
- Убедиться, что containerPort совпадает

### Ошибка 4: "Database connection failed"

**Причина:** Неправильная DATABASE_URL или пароль.

**Решение:**
- Проверить DATABASE_URL в переменных окружения
- Убедиться, что DB_PASSWORD установлен в Secrets
- Проверить пароль в Amvera консоли

---

## 📞 ИТОГОВАЯ РЕКОМЕНДАЦИЯ

### Для Backend:
```
Окружение: docker
Инструмент: docker
dockerfile: backend/Dockerfile
context: .
image: ${BUILD_IMAGE}
command: skip
persistenceMount: (удалите или оставьте пусто)
containerPort: 8000
```

### Для Frontend:
```
Окружение: docker
Инструмент: docker
dockerfile: frontend/Dockerfile
context: .
image: ${BUILD_IMAGE}
command: skip
persistenceMount: (удалите или оставьте пусто)
containerPort: 80
```

---

**Вот и всё! Теперь можно нажимать Deploy! 🚀**

Если что-то непонятно, обратитесь к полному руководству в `AMVERA_CONFIGURATION.md`.
